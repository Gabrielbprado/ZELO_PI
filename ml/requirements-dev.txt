-r requirements.txt

# pandas entra APENAS como sentinela do teste de memória: `test_no_pandas.py`
# bloqueia o import e prova que o caminho de serving funciona sem ele. Nenhum
# módulo do projeto (nem o treino) importa pandas — o pipeline usa numpy puro.
pandas~=2.2

pytest~=8.3
pytest-cov~=5.0
httpx~=0.27
ruff~=0.7
mypy~=1.13
